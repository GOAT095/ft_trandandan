import { Injectable } from '@nestjs/common';
import Authenticator from 'api42client';

@Injectable()
export class AuthService {
  // 42 authenticator instance
  // var app = new Authenticator(clientID, clientSecret, callbackURL);
}
